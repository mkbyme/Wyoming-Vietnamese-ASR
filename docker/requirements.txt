# Wyoming Vietnamese ASR Requirements

# Wyoming protocol
wyoming==1.5.0

# sherpa-onnx: pre-built wheel có sẵn trên PyPI (cp311 manylinux x86_64)
# KHÔNG dùng bare "sherpa-onnx" → pip sẽ cố build từ source nếu wheel không match
sherpa-onnx==1.12.32

# Audio processing
soundfile==0.12.1
numpy==1.26.2
scipy==1.11.4

# FastAPI server
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Utilities
requests==2.31.0
aiofiles==23.2.1
pydantic==2.5.0
pyyaml>=6.0

# HuggingFace download + CLI tools (cli extra deprecated từ v1.0+ nhưng không gây lỗi)
huggingface_hub[cli]>=0.20.0