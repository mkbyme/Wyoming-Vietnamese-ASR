# Wyoming protocol
wyoming==1.5.0

# ONNX Runtime — CUDA 12.6 (Ubuntu 24.04, Python 3.12)
onnxruntime-gpu==1.20.0

# sherpa-onnx — CPU wheel (GPU .so được override trong Dockerfile)
sherpa-onnx==1.12.32

# Audio
soundfile==0.12.1
numpy==1.26.4
scipy==1.11.4

# FastAPI
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Utils
requests==2.31.0
aiofiles==23.2.1
pydantic==2.5.0
pyyaml>=6.0
huggingface_hub[cli]>=0.20.0

# Punctuation restoration (xlm-roberta-capu) — GPU via CUDA
torch==2.5.1
transformers==4.46.0
sentencepiece==0.2.0